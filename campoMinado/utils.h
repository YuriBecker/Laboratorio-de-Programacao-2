#ifndef UTILS
#define UTILS

#include <time.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <unistd.h>

int getRandomNumber(int max);
int getInt(int min, int max, char str[]);

#endif